# TODO_DOC_DRIVERS

Remaining POD work for the main driver entrypoints.

## Done

- MySQL, PostgreSQL, and SQLite component modules now use a more consistent
  description of what the component does.
- replicated testing is documented across the three primary drivers.
- stale references to the old replicated namespace were cleaned earlier.

## Remaining

- compare the three component modules once more for sentence-level consistency
- decide later whether migration notes should stay in the component modules or
  move into deeper migration docs
- SQLite helper docs can still grow a little if the shared test story expands
